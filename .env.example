# API Configuration
# Protocol to use for API communication (http/https)
API_PROTOCOL=http

# Host address where the API is running
API_HOST=localhost

# Port number where the API is listening
API_PORT=8000

# Base URL for the backend API service
API_URL=http://localhost:8000

# Base path for the API if it's not at the root (e.g., /api/v1)
API_BASE_PATH=api

# Application Configuration
# Title displayed in the browser and app header
APP_TITLE=AI Knowledge Assistant

# Emoji icon used in the browser tab and app header
APP_ICON=🤖

# Layout mode: "wide" for full width or "centered" for narrower content
APP_LAYOUT=wide
